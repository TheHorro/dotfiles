return {
	-- Mini Nvim
	{ "nvim-mini/mini.nvim", 
    version = false,
    config = function()
      -- MiniAnimate (animations for scrolling and text operations)
      -- require("mini.animate").setup()

      -- MiniAI (improved text objects)
      -- require("mini.ai").setup()

      -- MiniBracketed (smart motions between brackets/tags)
      -- require("mini.bracketed").setup()

      -- MiniBufremove (buffer management)
      -- require("mini.bufremove").setup()

      -- MiniComment (commenting)
      -- require("mini.comment").setup({
        -- options = { ignore_blank_line = true },
        -- mappings = {
          -- comment = "gc", -- toggle comment
          -- comment_line = "gcc",
        -- },
      -- })

      -- MiniCompletion (completion engine)
      -- require("mini.completion").setup()

      -- MiniCursorword (highlight word under cursor)
      -- require("mini.cursorword").setup()

      -- MiniDoc (documentation generation)
      -- require("mini.doc").setup()

      -- MiniFiles (file explorer)
      -- require("mini.files").setup()

      -- MiniFuzzy (fuzzy finder)
      -- require("mini.fuzzy").setup()

      -- MiniJump (jump to characters or words)
      -- require("mini.jump").setup()

      -- MiniJump2d (2D jump within visible text)
      -- require("mini.jump2d").setup()

      -- MiniIndentscope (visualize indent levels)
      -- require("mini.indentscope").setup()

      -- MiniMove (move lines/blocks easily)
      -- require("mini.move").setup()

      -- MiniPairs (autopairs)
      -- require("mini.pairs").setup()

      -- MiniPalette (color palette for Neovim)
      -- require("mini.palette").setup()

      -- MiniSessions (sessions management)
      -- require("mini.sessions").setup()

      -- MiniSplitjoin (split/join code blocks)
      -- require("mini.splitjoin").setup()

      -- MiniStarter (start screen)
      -- require("mini.starter").setup()

      -- MiniStatusline (statusline)
      -- require("mini.statusline").setup()

      -- MiniSurround (surround text objects)
      -- require("mini.surround").setup()

      -- MiniTabline (tabline)
      -- require("mini.tabline").setup()

      -- MiniTrailspace (highlight/trimming trailing whitespace)
      -- require("mini.trailspace").setup()
    end,
  },
}
